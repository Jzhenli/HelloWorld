name: Build Linux App

on:
  push:
    tags:
      - '*-linux/v*'
      - '*-all/v*'
  workflow_dispatch:
    inputs:
      archs:
        description: 'Target architectures'
        required: false
        type: choice
        options: [armv7l, aarch64, 'armv7l,aarch64']
        default: 'armv7l'
      distro:
        description: 'Debian distro (bullseye=GLIBC 2.31, bookworm=GLIBC 2.36)'
        required: false
        type: choice
        options: [bullseye, bookworm]
        default: 'bullseye'
      enable_nuitka:
        description: 'Enable Nuitka compilation'
        required: false
        type: boolean
        default: true

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  config:
    runs-on: ubuntu-latest
    outputs:
      app_name: ${{ steps.vars.outputs.app_name }}
      version: ${{ steps.vars.outputs.version }}
      python_version: ${{ steps.vars.outputs.python_version }}
      build_matrix: ${{ steps.matrix.outputs.build_matrix }}
    steps:
      - uses: actions/checkout@v4

      - name: Read project config
        id: vars
        run: |
          APP_NAME=$(grep '^name = ' pyproject.toml | head -1 | sed 's/.*"\(.*\)".*/\1/')
          VERSION=$(grep '^version = ' pyproject.toml | head -1 | sed 's/.*"\(.*\)".*/\1/')
          PYTHON_VERSION=$(grep 'python_version' pyproject.toml | head -1 | sed 's/.*"\(.*\)".*/\1/')

          echo "app_name=$APP_NAME" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "python_version=$PYTHON_VERSION" >> $GITHUB_OUTPUT

      - name: Generate matrix
        id: matrix
        run: |
          ARCHS="${{ inputs.archs || 'armv7l' }}"
          ARCHS=$(echo "$ARCHS" | tr -d ' ')
          MATRIX=""

          [[ "$ARCHS" == *"armv7l"* ]]   && MATRIX+='{"arch":"armv7l","qemu_arch":"armv7"},'
          [[ "$ARCHS" == *"aarch64"* ]] && MATRIX+='{"arch":"aarch64","qemu_arch":"aarch64"},'

          echo "build_matrix=[${MATRIX%,}]" >> $GITHUB_OUTPUT

  build:
    needs: config
    runs-on: ubuntu-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.config.outputs.build_matrix) }}

    steps:
      - uses: actions/checkout@v4

      - name: Build on ${{ matrix.arch }}
        uses: uraimo/run-on-arch-action@v3
        with:
          arch: ${{ matrix.qemu_arch }}
          distro: ${{ inputs.distro || 'bullseye' }}
          githubToken: ${{ github.token }}
          install: |
            apt-get update -qq
            PY_VER="${{ needs.config.outputs.python_version }}"
            # Add bookworm repos for Python 3.10+, trixie for Python 3.12
            case "$PY_VER" in
              3.12) echo "deb http://deb.debian.org/debian trixie main" >> /etc/apt/sources.list ;;
              3.11|3.10) echo "deb http://deb.debian.org/debian bookworm main" >> /etc/apt/sources.list ;;
            esac
            apt-get update -qq
            apt-get install -y -qq python${PY_VER} python3-pip python${PY_VER}-venv build-essential libffi-dev libssl-dev \
              libbz2-dev libreadline-dev libsqlite3-dev zlib1g-dev \
              wget git file ca-certificates tar gzip gcc g++ make patchelf
            ln -sf /usr/bin/python${PY_VER} /usr/bin/python3
          run: |
            # Use piwheels for armv7 pre-compiled wheels
            if [ "${{ matrix.arch }}" = "armv7l" ]; then
              export PIP_EXTRA_INDEX_URL="https://www.piwheels.org/simple"
            fi

            python3 -m pip install --break-system-packages --upgrade pip
            python3 -m pip install --break-system-packages "pyapp-cli[compile] @ git+https://github.com/Jzhenli/pyapp.git"
            pyapp build linux --arch ${{ matrix.arch }}

            if [[ "${{ inputs.enable_nuitka }}" != "false" ]]; then
              pyapp compile linux
            fi

            pyapp package linux

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ${{ needs.config.outputs.app_name }}-${{ needs.config.outputs.version }}-linux-${{ matrix.arch }}
          path: dist/*.tar.gz

      - name: Summary
        if: always()
        run: |
          echo "### Linux Build (${{ matrix.arch }})" >> $GITHUB_STEP_SUMMARY
          echo "- **Version**: ${{ needs.config.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Arch**: ${{ matrix.arch }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Nuitka**: ${{ inputs.enable_nuitka != false }}" >> $GITHUB_STEP_SUMMARY
